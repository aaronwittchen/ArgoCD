# Examples

Reference configurations (not applied by default).

## Files

- `notifications-discord.yaml` - Discord webhook notifications
- `project-homelab.yaml` - AppProject template
- `sops-plugin.yaml` - Alternative SOPS plugin config
