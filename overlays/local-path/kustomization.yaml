apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - ../../base
# ArgoCD uses emptyDir by default for repo-server
# No PVC patches needed for local-path

# Environment-specific HTTPRoute configuration (Gateway API)
# patches:
#  - target:
#      kind: HTTPRoute
#      name: argocd-server
#    patch: |-
#      - op: replace
#        path: /spec/hostnames/0
#        value: argocd.yourdomain.com  # Update to your domain
