apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - ../../base
# ArgoCD uses emptyDir by default for repo-server
# Optionally, you can add persistent storage for Redis
# See examples/redis-persistent.yaml

# Environment-specific ingress configuration
# Update the hostname to match your environment
#patches:
#  - target:
#      kind: Ingress
#      name: argocd-server
#    patch: |-
#      - op: replace
#        path: /spec/tls/0/hosts/0
#        value: argocd.yourdomain.com  # TODO: Update to your domain
#      - op: replace
#        path: /spec/rules/0/host
#        value: argocd.yourdomain.com  # TODO: Update to your domain
